<?xml version="1.0" encoding="utf-8"?>
<!-- VT3-achtige indeling voor VT4 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tallyRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Speech Log (bovenaan, vaste hoogte) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewSpeechLog"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:background="@drawable/border_button"
        android:padding="4dp"
        android:scrollbars="vertical" />

    <!-- Knoppen rij -->
    <LinearLayout
        android:id="@+id/buttonsRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingTop="8dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonEndSession"
            style="@style/Widget.VoiceTally4.Button.Outlined"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_margin="4dp"
            android:layout_weight="1"
            android:text="Save" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonAddSpecies"
            style="@style/Widget.VoiceTally4.Button.Outlined"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_margin="4dp"
            android:layout_weight="1"
            android:text="Add" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonExportObservations"
            style="@style/Widget.VoiceTally4.Button.Outlined"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_margin="4dp"
            android:layout_weight="1"
            android:text="Export" />
    </LinearLayout>

    <!-- Tellers (vult de rest) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTally"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="4dp"
        android:scrollbars="vertical" />

</LinearLayout>
