<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Outlined tappable button: nu met stateful tint/text/stroke voor checked/unchecked -->
    <style name="VoiceTally.TappableButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:foreground">?attr/selectableItemBackground</item>

        <!-- State-lijsten -->
        <item name="backgroundTint">@color/selector_button_bg</item>
        <item name="android:textColor">@color/selector_button_text</item>
        <item name="strokeColor">@color/selector_button_stroke</item>
        <item name="strokeWidth">@dimen/tappable_stroke_1</item>

        <item name="android:textAllCaps">false</item>

        <!-- Compacte knoppen zoals in jouw layout -->
        <item name="android:minHeight">30dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
    </style>

    <!-- Filled variant (voor + en Save) -->
    <style name="VoiceTally.TappableButton.Filled" parent="Widget.Material3.Button">
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="strokeColor">@color/selector_button_stroke</item>
        <item name="strokeWidth">@dimen/tappable_stroke_1</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
